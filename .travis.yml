sudo: false
language: ruby
rvm:
  - 2.1.5
  - 2.2.2
  - 2.2.3
  - ruby-head
matrix:
  allow_failures:
    - rvm: ruby-head
  fast_finish: true
before_install: gem install bundler -v 1.10.6
script:
  - bundle exec rake spec
  - bundle exec rubocop --fail-level C
